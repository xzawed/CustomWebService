# worktrees
.worktrees/

# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/

# env files
.env
.env.local
.env.*.local

# railway
.railway

# IDE
.vscode/
.idea/

# Secret files — never commit credentials
*.pem
*.key
*.p12
*.pfx
secrets/
credentials/
.mcp.json
.env.production
.env.staging
.env.test*
*.tfstate*

# OS
.DS_Store
Thumbs.db

# debug
npm-debug.log*
pnpm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# test coverage
coverage/

# playwright
playwright-report/
test-results/

# drizzle-kit output: pg 경로는 재생성(미커밋); sqlite는 런타임 마이그레이션이라 커밋한다
/drizzle/*
!/drizzle/sqlite/

# SQLite DB 파일 (DB_PROVIDER=sqlite) — 스키마/마이그레이션만 커밋, 데이터는 커밋 금지
/data/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# claude — settings.local.json(시크릿)만 제외, settings.json(훅/권한)은 버전 관리
.claude/settings.local.json

# 임시 검증 산출물
verification-report.json
verify.log
